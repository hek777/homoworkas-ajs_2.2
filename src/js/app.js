const game = new Game();
game.start();

import loadGame, {Game as loadGame, GameSavingData as loadGame} from './game.js';
import saveGame, {readGameSaving as saveGame, writeGameSaving as saveGame} from './game.js';